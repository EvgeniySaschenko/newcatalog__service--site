import { common } from './common';
import { ratings } from './ratings';
import { sections } from './sections';
import { settings } from './settings';

export const $dbTemporary = { common, ratings, sections, settings };
